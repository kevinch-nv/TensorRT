[project]
name = "tensorrt-tests"
version = "0.1.0"
description = ""
dependencies = [ "numpy", "onnx", "onnxruntime==1.19.2", "protobuf>=3.20.2", "pytest", "ml_dtypes",]
requires-python = ">=3.10,<3.13"
[[project.authors]]
name = "TensorRT"
email = "svc_tensorrt@nvidia.com"

[build-system]
requires = [ "poetry-core>=1.3.0",]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
